<UserControl xmlns:Views="clr-namespace:RecepiesClient.Views"  x:Class="RecepiesClient.Views.LoginRegisterFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock>Login form</TextBlock>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="19*"/>
                <ColumnDefinition Width="56*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Username: " />
            <TextBox Text="{Binding Username}" Grid.Row="0" Grid.Column="1" Margin="30,0" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Password: " />
            <PasswordBox Name="BoxPassword" Grid.Row="1" Grid.Column="1" Margin="30,0" />
        </Grid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Login"
                    Command="{Binding Login}" 
                    CommandParameter="{Binding ElementName=BoxPassword}" />
            <Button Content="Register"
                    Command="{Binding Register}" 
                    CommandParameter="{Binding ElementName=BoxPassword}" />
        </StackPanel>
    </StackPanel>
</UserControl>
